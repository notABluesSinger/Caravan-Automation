# Repository Instructions

- This repository does not use pull requests. Do not create PRs for this repo.
- When explicitly asked to merge a completed branch into `main`, merge locally and push `main` directly only after the usual validation gates pass.
